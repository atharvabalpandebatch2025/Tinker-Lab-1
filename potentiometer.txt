// C++ code
//
int LED1 = 7;
int LED2 = 2;
int POT1 = A2;
int potValue = 0;
int brightness = 0;
void setup()
{
  Serial.begin(9600);
  
  pinMode(LED1, OUTPUT);
  pinMode(LED2, OUTPUT);
}

void loop()
{
  potValue = analogRead(POT1);
  brightness = potValue / 6;
  Serial.println(potValue);
  
  if (potValue <=511)
  {
    digitalWrite(LED1, HIGH);
      digitalWrite(LED2, LOW);
  }
  else
  {digitalWrite(LED1, LOW);
   digitalWrite(LED2, HIGH);}
   
  
}